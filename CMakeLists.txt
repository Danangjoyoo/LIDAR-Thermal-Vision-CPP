cmake_minimum_required(VERSION 2.8)
project( lidar-thermal-vision )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( lidar-thermal-vision main.cpp )
target_link_libraries( lidar-thermal-vision ${OpenCV_LIBS} )